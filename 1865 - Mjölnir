#include <stdio.h>
#include <string.h>

int main(){
	int c,n;
	char nome[10];
	
	scanf("%d", &c);
	while(c != 0){
		scanf("%s %d", nome,&n);
		if(strcmp(nome,"Thor") == 0)
			printf("Y\n");
		else
			printf("N\n");
		c--;
	}
	
	return 0;
}
