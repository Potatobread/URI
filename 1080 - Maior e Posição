#include <stdio.h>

int main(){
  int n,x,lugar,maior=0;
  
  n = 0;
  while(n != 100){
    scanf("%d", &x);
    if(x > maior){
      maior = x;
      lugar = n+1;
    }
    n++;
  }
  printf("%d\n", maior);
  printf("%d\n", lugar);
  
  return 0;
}
