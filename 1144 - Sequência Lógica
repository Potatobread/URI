#include <stdio.h>

int main(){
	int n,a,b=1,c,d;
	
	scanf("%d", &n);
	for(a=1;a<=n*2;a++){
		c = b*b;
		d = b*b*b;
		if(a % 2 == 0){
			c++;
			d++;
			printf("%d %d %d\n",b,c,d);
			b++;
		}
		else
			printf("%d %d %d\n",b,c,d);
	}
	
	return 0;
}
