#include <stdio.h>

int main(){
	int x,y,n;
	float divisao;

	scanf("%d", &n);
	while(n != 0){
		scanf("%d %d", &x, &y);
		if(y == 0)
			printf("divisao impossivel\n");
		else{
			divisao = x / (float)y;
			printf("%.1f\n",divisao);
		}
		n--;
	}
	
	return 0;
}
