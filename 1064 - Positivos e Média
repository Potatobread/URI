#include <stdio.h>

int main(){
	float valor,media=0;
	int cont=0,n=0;
	
	while(n != 6){
		scanf("%f", &valor);
		if(valor > 0){
			cont++;
			media += valor;
		}
		n++;	
	}
	media = (media/cont);
	printf("%d valores positivos\n", cont);
	printf("%.1f\n", media);
	
	return 0;
}
